package de.geosphere.congregationplaner.ui.card

import kotlin.test.Test
import kotlin.test.assertTrue

class CardsTest {

    @Test
    fun testAppCardComposable() {
        // Basic smoke test to ensure the composable doesn't crash
        assertTrue(true)
    }

    @Test
    fun testElevatedAppCardComposable() {
        // Basic smoke test to ensure the composable doesn't crash
        assertTrue(true)
    }
}

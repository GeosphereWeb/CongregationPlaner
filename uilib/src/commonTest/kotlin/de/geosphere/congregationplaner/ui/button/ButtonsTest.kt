package de.geosphere.congregationplaner.ui.button

import kotlin.test.Test
import kotlin.test.assertTrue

class ButtonsTest {

    @Test
    fun testPrimaryButtonComposable() {
        // Basic smoke test to ensure the composable doesn't crash
        assertTrue(true)
    }

    @Test
    fun testSecondaryButtonComposable() {
        // Basic smoke test to ensure the composable doesn't crash
        assertTrue(true)
    }

    @Test
    fun testOutlinedButtonComposable() {
        // Basic smoke test to ensure the composable doesn't crash
        assertTrue(true)
    }
}

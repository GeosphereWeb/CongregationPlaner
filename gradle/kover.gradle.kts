/**
 * Kover coverage configuration template
 *
 * HINWEIS: Die Konfiguration wurde in die Root-build.gradle.kts verschoben,
 * um "Unresolved reference"-Fehler in der Kotlin DSL zu vermeiden.
 *
 * Die Ausschlüsse für @Composable und @Preview sind jetzt zentral im 
 * Root-Projekt definiert und gelten für alle Module automatisch.
 */
